#include<stdio.h>
int main()
{
    scanf("%lld",&t);
    while(t--)
    {
        scanf("%lld %lld",&n,&m);
        for(i=0;i<n;i++)
        scanf("%s",s[i]);
        for(i=0;i<n;i++)
        {
            for(j=0;j<m;j++)
            {
                if(j==0)
                {
                    a[i][]=''
